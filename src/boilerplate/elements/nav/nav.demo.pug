extends ../../demo/_template

block title
    | KR-Nav

block example
    nav.kr-nav-main
        ul
            li
                a(href='#') First One
            li
                a(href='#') First Two
                ul
                    li
                        a(href='#') Second One
                    li
                        a(href='#') Second Two
                    li
                        a(href='#').-is-active Second Three
            li
                a(href='#') First Three
            li
                a(href='#') First Four
                ul
                    li
                        a(href='#') Second Four
                    li
                        a(href='#') Second Five
                    li
                        a(href='#') Second Six
            li
                a(href='#') First Five

block additional
    h2 SCSS
    code.
        $nav-main-enabled                       : true;                // KR-Menü de-/aktivieren

        $nav-main-active-class                  : "-is-active";        // CSS Klasse für aktive Menü-Elemente
        $nav-main-breakpoint                    : 768px;               // Breakpoint für Burger/mobiles Menü
        $nav-main-min-height                    : $grid-spacing * 2;   // Mindesthöhe der Menü-Elemente

        $nav-main-item-1-color                  : #fff;
        $nav-main-item-1-color-hover            : $nav-main-item-1-color;
        $nav-main-item-1-color-active           : $nav-main-item-1-color;
        $nav-main-item-1-background-color       : #808080;
        $nav-main-item-1-background-color-hover : #9a9a9a;
        $nav-main-item-1-background-color-active: #9a9a9a;

        $nav-main-item-2-color                  : $nav-main-item-1-color;
        $nav-main-item-2-color-hover            : $nav-main-item-2-color;
        $nav-main-item-2-color-active           : $nav-main-item-2-color;
        $nav-main-item-2-background-color       : $nav-main-item-1-background-color;
        $nav-main-item-2-background-color-hover : $nav-main-item-1-background-color-hover;
        $nav-main-item-2-background-color-active: $nav-main-item-1-background-color-hover;

        $nav-main-item-separator                : 1px solid #fff;      // Abstandselement zwischen den Menüpunkten
        $nav-main-burger-patty-color            : #333;                // Farbe der Burger-Elemente

        $nav-main-trigger-color                 : #ddd;                // Farbe des mobilen Aufklapp-Symbols (Chevron)
        $nav-main-trigger-background-color      : rgba(#fff, .25);
        $nav-main-trigger-radius                : 4px;

    h2 JavaScript
    code.
        $('.kr-nav-main').krNav({
            closeOnResize: true,             // Navigation beim scrollen schließen
            scroll       : true,             // Click auf Burger scrollt zu Menü anfang
            scrollOffset : 0,                // Wenn "scroll: true"; scroll abstand zum Menü
            scrollSpeed  : 600,              // Wenn "scroll: true"; Scrollgeschwindigkeit
            slideSpeed   : 'slow',           // Scrollgeschwindigkeit des mobilen Menüs
            activeClass  : '-is-active',     // CSS Klasse für aktive Elemente
            expandedClass: '-is-expanded',   // CSS Klasse für aufgeklappte Elemente
            breakpoint   : 768               // Breakpoint für Burger/mobiles Menü
        });

    p.hint Wenn der <b>$nav-main-breakpoint</b> und/oder <b>$nav-main-active-class</b> manuell gesetzt werden, muss dieser auch in der JavaScript Instanz gesetzt werden.


block script
    script.
        $('.kr-nav-main').krNav({
            scrollOffset: -50
        });
